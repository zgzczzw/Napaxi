<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Local A2A transport: LAN discovery and JSON-lines peer messaging. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <!-- Platform tools: location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Platform tools: notification (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Platform tools: contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Platform tools: calendar -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- Platform tools: camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Platform tools: media library -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Platform tools: audio recording -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Device channels: Bluetooth headset host transport hooks. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Platform tools: alarm (system clock intent) -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <!-- Platform tools: APK installation. Android still requires user confirmation. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="text/html" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/xhtml+xml" />
        </intent>
        <intent>
            <action android:name="agent.provider.action.INSTALL_AGENT" />
        </intent>
        <intent>
            <action android:name="agent.provider.action.HANDLE_PROPOSAL" />
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <!-- Background agent: foreground service + wake lock -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application>
        <!-- Foreground service for background agent execution -->
        <service
            android:name="com.napaxi.flutter.NapaxiAgentService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="AI Agent execution" />
        </service>

        <service
            android:name="com.napaxi.flutter.AgentTriggerIngressService"
            android:exported="true" />

        <!-- Receiver for notification action buttons -->
        <receiver
            android:name="com.napaxi.flutter.NapaxiActionReceiver"
            android:exported="false" />

        <receiver
            android:name="com.napaxi.flutter.NapaxiAutomationWakeReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.napaxi_flutter.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/napaxi_file_paths" />
        </provider>
    </application>

</manifest>
